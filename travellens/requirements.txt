# TravelLens LK -- runtime dependencies
pandas>=1.5
numpy>=1.23
scipy>=1.9
scikit-learn>=1.1
transformers>=4.40
torch>=2.0
sentence-transformers>=2.2
requests>=2.28

# Only for scripts/37_annotation_workbook.py, which builds the Excel
# sheet for hand-labelling the gold set. Nothing in the pipeline needs it.
openpyxl>=3.1
