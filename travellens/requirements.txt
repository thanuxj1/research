# TravelLens LK -- runtime dependencies
fastapi>=0.111
uvicorn[standard]>=0.29
pandas>=1.5
numpy>=1.23
scipy>=1.9
scikit-learn>=1.1
transformers>=4.40
torch>=2.0
sentence-transformers>=2.2
requests>=2.28

# Only for scripts/37_annotation_workbook.py, which builds the Excel
# sheet for hand-labelling the gold set. Nothing in the pipeline needs it.
openpyxl>=3.1

# User-submission storage: local SQLite by default; this is used only when
# SUBMISSIONS_DATABASE_URL is set (Postgres/Neon for a hosted deploy).
psycopg2-binary

# .env loading for local development (scripts/41_serve_api.py). A hosted
# deploy sets real environment variables and does not need this; load_dotenv
# never overrides a variable that's already set, so that path is untouched.
python-dotenv
